/**
 * Shared lucide-react defaults for dashboard / clinical UI icons.
 * Keep stroke weight consistent app-wide (UI only — not PDFs).
 */
export const ICON_STROKE = 1.5;

/** Nav chrome (sidebar, header, mobile nav) */
export const ICON_SIZE_NAV = "h-[18px] w-[18px] shrink-0";

/** Inline actions / table affordances */
export const ICON_SIZE_SM = "h-3.5 w-3.5 shrink-0";

/** Default body / card icons */
export const ICON_SIZE_MD = "h-4 w-4 shrink-0";

/** Stats / feature callouts */
export const ICON_SIZE_LG = "h-5 w-5 shrink-0";

/** Empty states / large placeholders */
export const ICON_SIZE_XL = "h-8 w-8 shrink-0";
